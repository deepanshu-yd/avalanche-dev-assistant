{
  "name": "avalanche-dev-assistant-monorepo",
  "private": true,
  "workspaces": [
    "server",
    "extension"
  ],
  "scripts": {
    "build": "npm run build -w server && npm run build -w extension",
    "clean": "rimraf server/dist extension/dist",
    "dev:server": "npm run dev -w server"
  },
  "devDependencies": {
    "rimraf": "^6.0.1"
  }
}
